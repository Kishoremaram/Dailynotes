# Python Program to Count Occurrences of Element in List
def occur(a):
        b=2
        count=0
        for i in a:
                if i==b:
                        count=count+1
        return count

